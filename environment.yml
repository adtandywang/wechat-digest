name: wechat-digest
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - requests>=2.28,<3
      - feedparser>=6.0,<7
      - PyYAML>=6.0,<7
      - python-dateutil>=2.8,<3
      - pytest>=7.0,<9
